<h1>Flight Search</h1>

<form action="/flights" method="get" class="search-form">
  <input type="text" name="origin" placeholder="Origin" required />
  <input type="text" name="destination" placeholder="Destination" required />
  <input type="date" name="date" required />
  <button type="submit">Search</button>
</form>

{{#if flights}}
  <h2>Search Results</h2>
  {{#if flights.length}}
    <ul class="flight-list">
      {{#each flights}}
        <li>
          Flight: {{flightNumber}} | {{origin}} → {{destination}}<br />
          Departure: {{departureTime}} | Price: ${{price}}<br />
          <a href="/bookings/new/{{_id}}">Book Now</





ChatGPT said:
a>
</li>
{{/each}}
</ul>
{{else}}
<p>No flights found.</p>
{{/if}}
{{/if}}

php-template
Copy code

---

### 9. **views/bookingForm.hbs**

```handlebars
<h1>Booking Flight: {{flight.flightNumber}}</h1>

<form action="/bookings/new/{{flight._id}}" method="post" class="booking-form">
  <label>Name:</label>
  <input type="text" name="passengerName" required />

  <label>Email:</label>
  <input type="email" name="passengerEmail" required />

  <label>Seats to Book:</label>
  <input type="number" name="seatsBooked" min="1" max="{{flight.seatsAvailable}}" required />

  <button type="submit">Confirm Booking</button>
</form>

<p>Seats available: {{flight.seatsAvailable}}</p>